# Render Blueprint — single web service that builds the React frontend and
# serves it together with the FastAPI backend from one combined Docker image
# (see ./Dockerfile). One URL hosts the whole application.
services:
  - type: web
    name: gym-reporter-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
